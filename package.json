{
  "name": "infected-voices-windows",
  "version": "0.7.0",
  "private": true,
  "description": "Windows WebView2 shell for Infected Voices Core. Plans and DSP stay in Core.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "sync": "node scripts/sync-core.mjs",
    "sync:build": "node scripts/sync-core.mjs --build",
    "build": "node scripts/build.mjs",
    "release": "node scripts/build.mjs --release",
    "sign": "node scripts/build.mjs --sign",
    "test": "node --test scripts/honesty.test.mjs"
  }
}
